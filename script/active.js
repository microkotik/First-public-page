document.addEventListener('DOMContentLoaded', function () { // После загрузки документа
  let nameWrapper = document.querySelector('.name');
  nameWrapper.classList.add("active");
  let priceWrapper = document.querySelector('.price');
  priceWrapper.classList.add("active")
});